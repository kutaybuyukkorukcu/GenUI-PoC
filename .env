# Semantic Kernel Configuration
SEMANTIC_KERNEL_PROVIDER=Ollama

# OpenAI Configuration (not used, but included for reference)
OPENAI_API_KEY=sk-mock-key-not-used
OPENAI_MODEL_ID=gpt-4o-mini

# Azure OpenAI Configuration (not used, but included for reference)
AZURE_OPENAI_ENDPOINT=https://mock-endpoint.openai.azure.com
AZURE_OPENAI_API_KEY=mock-key-not-used
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4.1-mini

# Ollama Configuration (ACTIVE)
OLLAMA_ENDPOINT=http://host.docker.internal:11434/v1
OLLAMA_MODEL_ID=llama3.2:latest
